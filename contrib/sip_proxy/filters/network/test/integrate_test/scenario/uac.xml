<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE scenario SYSTEM "sipp">

<scenario name="Sip Outgoing Call User A">
  <!-- In client mode (sipp placing calls), the Call-ID MUST be         -->
  <!-- generated by sipp. To do so, use [call_id] keyword.              -->

  <send retrans="500">
    <![CDATA[
        INVITE sip:[field1]@[field6] SIP/2.0
        Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
        From: <sip:[field0]@[field6]>;tag=[call_number]
        To: <sip:[field1]@[field6]>
        Call-ID: [call_id]
        CSeq: [cseq] INVITE
        Supported: 100rel
        Route: <sip:[field3]:[field2];role=anch;lr;transport=tcp;x-suri=sip:icscf-internal.cncs.svc.cluster.local:15090;xafi=13001;lskpmc=S1F1>
        Record-Route: <sip:[field3]:[field2];role=anch;lr;transport=tcp;x-suri=sip:pcsf-cfed.cncs.svc.cluster.local:15090;xkey=001>
        Contact: <sip:[field3]:[field2];role=anch;lr;transport=tcp;x-suri=sip:pcsf-cfed.cncs.svc.cluster.local:15090>
        P-Asserted-Identity: <sip:[field0]@[field6]>
        Allow: UPDATE,INVITE,ACK,CANCEL,BYE,PRACK,REFER,MESSAGE,INFO
        Max-Forwards: 70
        Content-Type: application/sdp
        Content-Length:[len]

        v=0
        o=PCTEL 256 2 IN IP[local_ip_type] [local_ip]
        c=IN IP[media_ip_type] [media_ip]
        m=audio 4030 RTP/AVP 0 8
        a=rtpmap:0 PCMU/8000
        a=rtpmap:8 PCMU/8000
    ]]>
  </send>

  <recv response="100" rtd="true" optional="true">
  </recv>

  <recv response="180" rtd="true" rrs="true" optional="true">
  </recv>

  <recv response="183" rtd="true" rrs="true" optional="true">
  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->

  <recv response="200" rtd="true" rrs="true" crlf="true">
  </recv>

  <!-- Packet lost can be simulated in any send/recv message by         -->
  <!-- by adding the 'lost = "10"'. Value can be [1-100] percent.       -->

  <send>
    <![CDATA[
         ACK [next_url] SIP/2.0
         Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
         [routes]
         [last_From:]
         [last_To:]
         [last_Call-ID:]
         [routes]
         CSeq: [cseq] ACK
         Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport];x-suri=sip:pcsf-cfed.cncs.svc.cluster.local:15090>
         Max-Forwards: 70
         Content-Length: [len]
    ]]>
  </send>

  <!-- This delay can be customized by the -d command-line option       -->
  <!-- or by adding a 'milliseconds = "value"' option here.             -->
  <!--pause milliseconds="1500"/-->

  <pause milliseconds="3000"/>
  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via:SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [routes]
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport];x-suri=sip:pcsf-cfed.cncs.svc.cluster.local:15090>
      CSeq: [cseq] BYE
      Max-Forwards: 70
      Content-Length: [len]
    ]]>
  </send>

  <!-- The 'crlf' option inserts a blank line in the statistics report. -->

  <recv response="200"  rtd="true" rrs="true" optional="true">
  </recv>


  <!-- definition of the response time repartition table (unit is ms)   -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

  <!-- definition of the call length repartition table (unit is ms)     -->
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
